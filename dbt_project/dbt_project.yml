name: nyc_taxi
version: "1.0.0"
profile: nyc_taxi
model-paths: ["models"]
test-paths: ["tests"]

models:
  nyc_taxi:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: incremental
      +incremental_strategy: merge
      +schema: marts
